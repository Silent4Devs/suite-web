FROM dunglas/frankenphp

# Agrega el script de instalación de extensiones PHP
ADD https://github.com/mlocati/docker-php-extension-installer/releases/latest/download/install-php-extensions /usr/local/bin/

# Cambia los permisos del script para hacerlo ejecutable
RUN chmod +x /usr/local/bin/install-php-extensions

# Instalar dependencias y extensiones PHP
RUN apt-get update && apt-get install -y --no-install-recommends \
    bash \
    curl \
    libfreetype6-dev \
    g++ \
    gcc \
    git \
    libicu-dev \
    libc6-dev \
    libzip-dev \
    libpq-dev \
    make \
    nodejs \
    npm \
    libonig-dev \
    yarn \
    openssh-client \
    rsync \
    zlib1g-dev \
    sudo \
    zip \
    unzip \
    libsodium-dev \
    && install-php-extensions \
    @composer \
    redis-stable \
    bcmath \
    calendar \
    exif \
    gd \
    intl \
    pdo_pgsql \
    pcntl \
    soap \
    zip \
    apcu \
    opcache \
    sockets \
    sodium \
    excimer \
    posix \
    amqp \
    ftp \
    && apt-get clean && rm -rf /var/lib/apt/lists/*

# Establecer permisos
RUN chown -R www-data:www-data /var/www && chmod 755 -R /var/www

# Instalar la última versión de npm
RUN npm install -g npm@latest
