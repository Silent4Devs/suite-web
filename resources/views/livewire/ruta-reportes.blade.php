<div>
    @if()
        {{ Breadcrumbs::render('admin.portal-comunicacion.reportes') }}
    @endif

    @if()
        {{ Breadcrumbs::render('admin.portal-comunicacion.reportes') }}
    @endif

    @if()
        {{ Breadcrumbs::render('admin.portal-comunicacion.reportes') }}
    @endif
</div>
